<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>ELOHH 💗</title>

<style>
* { box-sizing: border-box; }

body {
    margin: 0;
    font-family: 'Segoe UI', cursive;
    background: linear-gradient(180deg, #ffc0cb, #ff69b4, #ff1493);
    min-height: 100vh;
    overflow-x: hidden;
}

/* CORAÇÕES */
.heart {
    position: fixed;
    animation: float 7s linear infinite;
    pointer-events: none;
    opacity: 0.6;
}

@keyframes float {
    0% { transform: translateY(110vh) scale(0.7); }
    100% { transform: translateY(-10vh) scale(1.3); opacity: 0; }
}

/* TÍTULO */
h1 {
    text-align: center;
    font-size: 42px;
    margin: 25px 10px 10px;
    background: linear-gradient(45deg, #fff, #ffb6c1, #ff1493);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    text-shadow: 0 0 15px rgba(255,105,180,0.8);
}

.subtitle {
    text-align: center;
    color: white;
    font-size: 18px;
    margin-bottom: 25px;
}

/* CONTAINER */
.container {
    margin: 0 12px 30px;
    background: rgba(255,255,255,0.25);
    backdrop-filter: blur(12px);
    padding: 22px;
    border-radius: 25px;
    box-shadow: 0 0 30px rgba(255,20,147,0.6);
}

/* FOTOS — SEMPRE EM COLUNA */
.photos {
    display: flex;
    flex-direction: column; /* FORÇADO */
    gap: 22px;
    align-items: center;
}

.photo {
    width: 100%;
    max-width: 100%;
    height: 230px;
    object-fit: cover;
    border-radius: 22px;
    border: 4px solid white;
    box-shadow: 0 0 18px rgba(255,105,180,0.9);
}

/* TEXTO */
.message {
    margin-top: 28px;
    text-align: center;
    color: white;
    font-size: 20px;
    line-height: 1.5;
    text-shadow: 1px 1px 6px #ff1493;
}

/* BOTÕES */
.music-box {
    margin-top: 35px;
    display: flex;
    flex-direction: column;
    gap: 18px;
}

.love-button {
    width: 100%;
    padding: 20px;
    font-size: 20px;
    border-radius: 50px;
    border: none;
    background: linear-gradient(135deg, #ff69b4, #ff1493);
    color: white;
    cursor: pointer;
    box-shadow: 0 0 30px rgba(255,20,147,1);
    animation: pulse 2s infinite;
    text-decoration: none;
    text-align: center;
}

.love-button.youtube {
    background: linear-gradient(135deg, #ffb6c1, #ff69b4);
}

@keyframes pulse {
    0% { box-shadow: 0 0 18px #ff69b4; }
    50% { box-shadow: 0 0 45px #ff1493; }
    100% { box-shadow: 0 0 18px #ff69b4; }
}

footer {
    text-align: center;
    color: white;
    margin: 25px 10px;
    font-size: 14px;
}
</style>
</head>

<body>

<script>
for (let i = 0; i < 40; i++) {
    const h = document.createElement("div");
    h.className = "heart";
    h.innerHTML = "💖";
    h.style.left = Math.random() * 100 + "vw";
    h.style.fontSize = (20 + Math.random() * 30) + "px";
    h.style.animationDuration = (4 + Math.random() * 6) + "s";
    document.body.appendChild(h);
}
</script>

<h1>ELOHH 💗</h1>
<p class="subtitle">Cada momento com você é eterno</p>

<div class="container">

<div class="photos">
    <img class="photo" src="1000001185.jpg">
    <img class="photo" src="1000006544.jpg">
    <img class="photo" src="1000012733.jpg">
</div>

<div class="message">
    <p>💌 Você é o meu lugar seguro.</p>
    <p>Com você, todos os dias são especiais 💕</p>
</div>

<div class="music-box">
    <button class="love-button" onclick="toggleMusica()">🎶 Tocar música</button>

    <a class="love-button youtube"
       href="https://youtu.be/j_a55VGxiEU"
       target="_blank">
       ▶️ Abrir vídeo no YouTube
    </a>
</div>

</div>

<footer>
Feito com amor infinito 💞
</footer>

<audio id="musica" loop>
    <source src="Carol e Junior - Grava essa ideia(MP3_160K).mp3" type="audio/mpeg">
</audio>

<script>
let tocando = false;
function toggleMusica() {
    const m = document.getElementById("musica");
    if (!tocando) { m.play(); tocando = true; }
    else { m.pause(); tocando = false; }
}
</script>

</body>
</html>
